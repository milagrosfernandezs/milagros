{
  "name": "cute-studio-egresados",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev dist --d1=DB --r2=DESIGNS",
    "db:init": "wrangler d1 execute cute-studio-egresados --local --file=schema.sql",
    "db:init:remote": "wrangler d1 execute cute-studio-egresados --remote --file=schema.sql"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2",
    "wrangler": "^3.75.0"
  }
}
